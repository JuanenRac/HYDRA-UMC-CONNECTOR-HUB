{
  "adapterId": "broken-literal-secret",
  "schemaVersion": "1.0",
  "protocol": "mqtt",
  "targetKinds": ["sensor"],
  "endpointSchema": { "type": "object", "properties": { "brokerHost": { "type": "string" } } },
  "authenticationRef": "token=hunter2",
  "capabilities": [
    { "name": "subscribeTopic", "mode": "read", "risk": "low" }
  ],
  "requiredSafetyGates": [],
  "healthCheck": { "type": "mqtt-ping" },
  "timeoutMs": 3000,
  "idempotency": "idempotent",
  "evidenceSchema": { "type": "object", "properties": { "topic": { "type": "string" } } },
  "sdkCompatibility": ">=0.0.9",
  "ownerProject": "HYDRA-UMC-MQTT-BROKER"
}
